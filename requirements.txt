# Phase 2 pipeline. Python 3.10+.
# Install on your own machine (this session's sandbox blocks the model CDNs;
# your normal internet does not).
sentence-transformers>=2.7.0
chromadb>=0.5.0
rank-bm25>=0.2.2
pdfplumber>=0.11.0
pypdf>=4.0.0
PyYAML>=6.0
numpy>=1.26
anthropic[vertex]>=0.39.0   # [vertex] pulls google-auth for the Vertex path
# poppler-utils (system package) is needed for pdftotext used by build_corpus.py
#   macOS:  brew install poppler
#   Debian: apt-get install poppler-utils
